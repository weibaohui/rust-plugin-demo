[package]
name = "plugkit-standalone"
version = "0.2.0"
edition = "2021"

[workspace]

[dependencies]
plugkit = { version = "0.2", path = "../.." }
axum = "0.7"
tokio = { version = "1", features = ["full"] }
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
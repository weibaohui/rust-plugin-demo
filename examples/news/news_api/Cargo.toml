[package]
name = "news_api"
version = "0.1.0"
edition = "2018"

[workspace]

[lib]
crate-type = ["dylib", "rlib"]

[dependencies]
plugkit = { version = "0.2", path = "../../.." }
log = "0.4"
include_dir = "0.7"
serde = { version = "1", features = ["derive"] }